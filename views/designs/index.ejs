<%- include('../partials/header') %>

<h1>All Designs</h1>

<table id="designList">

	<thead>
		<tr>
			<th>Inspo</th>
			<th>Style</th>
			<th>view</th>
		</tr>
	</thead>
	<tbody>
		<% designs.forEach(function(g) { %>
		<tr>
			<td><%= d.inspo %></td>
			<td><%= d.style %></td>
			<td><a href="/designs/<%= d._id %>">view</a></td>
		</tr>
		<% }); %>
	</tbody>
</table>

<%- include('../partials/footer') %>